<Window x:Class="TestApp.Time"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestApp"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Time" Height="600" Width="800" WindowStyle="ThreeDBorderWindow">
    <Grid Background="#efebfa">
        <Border MinWidth="400" Margin="15" Background="White" VerticalAlignment="Center" Padding="30" MaxWidth="550" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="60" Color="LightGray" ShadowDepth="0" />
            </Border.Effect>
            <StackPanel x:Name="stackPanel">
                <TextBlock Text="Measurement Time" FontSize="30" FontWeight="Light" Margin="0 0 0 20"/>
                <Grid x:Name="window1" Margin="0 0 0 30">
                    
                </Grid>
                
                <Grid Margin="0 0 0 20">
                    <Button HorizontalAlignment="Left" Content="Вычислить" Click="Result_Graph"/>
                </Grid>
            </StackPanel>
        </Border>

    </Grid>
</Window>
